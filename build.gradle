apply plugin: 'com.android.library'

android {
    compileSdkVersion 20
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "com.tughi.android.database.sqlite"
        minSdkVersion 1
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
    }
}
